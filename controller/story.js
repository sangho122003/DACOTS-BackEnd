const bcrypt = require('bcrypt');

const storyCrtl = {
   getStory: async (res,req)=>{
      res.send("User registered");
   },
   addStory: async(res,req)=>{
      res.send("User registered");
   },
   DeleteStory: async(res,req)=>{
      res.send("User registered");
   }
};

module.exports = storyCrtl;
