const bcrypt = require('bcrypt');

const commentCrtl = {
   getCommnet: async (res,req)=>{
      res.send("User registered");
   },
   addComment: async(res,req)=>{
      res.send("User registered");
   },
   updataComment: async(res,req)=>{
      res.send("User registered");
   },
   deleteComment: async(res,req)=>{
      res.send("User registered");
   }
};

module.exports = commentCrtl;
