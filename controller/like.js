const bcrypt = require('bcrypt');
const likeCtrl = {
   getLike: async (res,req)=>{
      res.send("User registered"); 
   },
   addLike: async(res,req)=>{
      res.send("User registered");
   },
   DeletePost: async(res,req)=>{
      res.send("User registered");
   }
};

module.exports = likeCtrl;
